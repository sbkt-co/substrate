# Pinned Ansible test toolchain for reproducible local + CI runs.
# Bump deliberately; CI and developers share these exact versions.
# ansible-core 2.18+ is required for controller Python 3.13 (Debian trixie).
ansible-core==2.18.6
ansible-lint==24.12.2
yamllint==1.35.1
